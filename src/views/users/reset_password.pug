extends ../layout/default

block content
    form(method="post")
        p Enter the email address of your account
        input(type="email", name="email_address", required)
        input(type="submit", value="Submit")